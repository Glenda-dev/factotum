[package]
name = "factotum"
version = "0.1.0"
edition = "2024"
description = "Authentication and Authorization Manager for Glenda"

[dependencies]
libglenda-rs = { path = "../../lib/libglenda-rs", features = ["rt-service"] }
